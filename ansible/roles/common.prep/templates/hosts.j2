{% for node in config.groups['cluster'] -%}
{{ config.nodes[node].networks.private }} {{ node }}.{{ config.nodes[node].domain }} {{ node }}
{% if 'public' in config.nodes[node].networks -%}
{{ config.nodes[node].networks.public }} {{ node }}_pub.{{ config.nodes[node].domain }} {{ node }}_pub
{% endif -%}
{% if config.nodes[node].ctdb and ('public' in config.nodes[node].ctdb) -%}
{{ config.nodes[node].ctdb.public }} {{ node }}_ctdb.{{ config.nodes[node].domain }} {{ node }}_ctdb
{% endif -%}
{% endfor -%}
